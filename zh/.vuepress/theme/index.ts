import { path } from '@vuepress/utils'
import { } from '@vuepress/utils'

export default {
  name: 'vuepress-theme-foo',
  layouts: {
    Layout: path.resolve(__dirname, './layouts/Layout.vue'),
  },
}